
1. creer une facture
    ($facture=new\Facture;)
    pour mettre les details:
        $facture->details="generateauto.";
            $facture->save();

2. affectation des consommations

//Ici on parcours les consommations
    $valeur=0;
    foreach($nouvelle_conso as $conso){
        $valeur=$valeur+$conso->valeur;
        oubien
        $valeur+=$conso->valeur;
    }
    $facture->$valeur_totale_consommee=$valeur; //enregistrement de la valeur
    $facture->montant=$valeur*3;


3. affectation client

4. calcul du montant

5. calcul de la valeur totale
